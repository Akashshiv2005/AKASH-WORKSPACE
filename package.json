{
  "name": "akash-workspace-root",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "npm run build --prefix frontend",
    "dev": "npm run dev --prefix frontend"
  },
  "dependencies": {
    "nodemailer": "^6.9.13"
  }
}
